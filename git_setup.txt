% git init --initial-branch=main

% git remote add main git@github.com:lumpynose/stripesbook-jsf.git

% git pull main main

% git push --set-upstream main main

% git add *

% git commit -m 'initial commit'

% git push

# instructions from github site for new repo:

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:lumpynose/stripesbook-jsf.git
git push -u origin main

# instructions from github for pushing existing repo:

git remote add origin git@github.com:lumpynose/stripesbook-jsf.git
git branch -M main
git push -u origin main
